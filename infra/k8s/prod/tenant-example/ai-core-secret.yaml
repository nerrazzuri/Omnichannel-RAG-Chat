apiVersion: v1
kind: Secret
metadata:
  name: ai-core-env
  namespace: tenant-example
type: Opaque
stringData:
  # Replace with your real keys before production use
  OPENAI_API_KEY: ""
  JWT_SECRET: "dev-secret-change-me-please-32-bytes-minimum"
  # Optionally override service URLs to managed services instead of in-cluster
  # DATABASE_URL: "postgresql://user:password@postgres:5432/omni"
  # REDIS_URL: "redis://redis:6379/0"
  # QDRANT_URL: "http://qdrant:6333"


