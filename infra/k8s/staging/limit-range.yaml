apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: omni-staging
spec:
  limits:
    - type: Container
      default:
        cpu: "1"
        memory: "512Mi"
      defaultRequest:
        cpu: "200m"
        memory: "256Mi"

